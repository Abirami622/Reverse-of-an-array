#include <stdio.h>

int main()
{
    int a[10], j;
    for(int i=0;i<=5; i++)
    {
        scanf("%d", &a[i]);
    }
    for(int i=0; i<=5; i++)
    {
        for(j=i+1; j<=5; j++)
        {
            int t=a[i];
            a[i]=a[j];
            a[j]=t;
        }
        }
        
    
    for(int i=0;i<=5; i++)
    {
        printf("%d ", a[i]);
    }
    
    
    return 0;
}
